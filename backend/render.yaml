services:
  - type: web
    name: ecommerce-backend
    env: node
    buildCommand: npm ci --only=production
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production